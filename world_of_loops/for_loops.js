// for (
//     [initial Expression];
//     [condition];
//     [Increament Expression];
// )

for (let i = 1; i <= 10; i++) {
  console.log("HELLO: ", i);
}

// Generating 4 perfect squares using for loops

for (let num = 1; num <= 20; num++) {
  //   console.log(num);
  console.log(`${num}x ${num} =${num * num}`);
}

// Reversed Iteration
for (let i = 200; i >= 0; i -= 25) {
  console.log("Reversed Iteration " + i);
}
